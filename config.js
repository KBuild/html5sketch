module.exports = function(conf) {
    return list[conf];
}

list = {
    mysql: {
        host:   "localhost",
        user:   "root",
        password:   "imsqlroot",
        database:   "test"
    }
}